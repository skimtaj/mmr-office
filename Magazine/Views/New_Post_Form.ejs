<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Registration</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #282C34;
            font-family: Arial, sans-serif;
        }
        .registration-card {
            background-color: #2C3E50;
            padding: 30px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            color: white;
            max-width: 400px;
            margin: 50px auto;
        }
        .registration-card h2 {
            text-align: center;
            margin-bottom: 20px;
            color: #fff;
        }
        .registration-card input, .registration-card textarea {
            margin-bottom: 15px;
            border-radius: 20px;
            border: none;
            padding: 10px;
            width: 100%;
            box-sizing: border-box;
        }
        .registration-card textarea {
            height: 100px;
            resize: none;
        }
        .registration-card button {
            background-color: #00A65A;
            color: white;
            border-radius: 20px;
            border: none;
            padding: 10px;
            width: 100%;
            font-weight: bold;
            margin-bottom: 10px;
        }
        .registration-card button:hover {
            background-color: #282C34;
            color: #fff;
        }

        .account-message {
            margin-top: 1rem;
            text-align: center;
        }
        .account-message a {
            color: #00a65a;
            text-decoration: none;
        }
        .account-message a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="registration-card">
        <h2> Mgazine Posting Form </h2>
        <form method="post">
            <div class="mb-3">
                <label for="title" class="form-label">Title</label>
                <input type="text" class="form-control" id="title" placeholder="Enter a title" name="Title">
            </div>
            <div class="mb-3">
                <label for="description" class="form-label">Description</label>
                <textarea class="form-control" id="description" placeholder="Enter a description" name="Description"></textarea>
            </div>
            <div class="mb-3">
                <label for="date" class="form-label">Date</label>
                <input type="date" class="form-control" id="date" name="Date">
            </div>
            <button type="submit">Submit</button>
            <div class="account-message">
                <a href="/Dashboard/">Back To Dashboard</a>
            </div>
        </form>
    </div>
</body>
</html>
