<!DOCTYPE html>
<html lang="en">


<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Responsive Sign-up Form</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css"
        integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous">

    <!-- CSS file -->
    <style>
        /* Google Fonts (Poppins) */
        @import url("https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap");

        * {
            font-family: "Poppins", sans-serif;
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            background: #34425A;
            /* Updated background color */
            padding: 20px;
        }

        .wrapper {
            width: 100%;
            max-width: 400px;
            background: #fff;
            border-radius: 10px;
            padding: 30px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        .wrapper h2 {
            margin-bottom: 20px;
            font-weight: 400;
            color: #22BAA0
        }

        .input {
            width: 100%;
            padding: 12px 15px;
            margin: 10px 0;
            background: #f0f0f0;
            border: none;
            border-radius: 5px;
            outline: none;
            font-size: 16px;
        }

        .btn {
            width: 100%;
            padding: 12px;
            background: #22BAA0;
            color: #fff;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 16px;
            margin-top: 20px;
            transition: background 0.3s;
        }

        .btn:hover {
            background: #357a85;
        }

        .back-link {
            display: block;
            margin-top: 20px;
            color: #3b8d99;
            text-decoration: none;
            font-size: 14px;
            transition: color 0.3s;
        }

        .back-link:hover {
            color: #22BAA0;
        }

        @media (max-width: 480px) {
            .wrapper {
                padding: 20px;
            }

            .input {
                padding: 10px 12px;
            }

            .btn {
                padding: 10px;
                font-size: 14px;
            }
        }
    </style>
</head>

<body>
    <div class="wrapper">
        <h2>Sign Up</h2>

        <% if(messages.success) { %>
            <div class="alert alert-success" role="alert" id='msg'>
                <%=messages.success%>
            </div>

            <% } %>

                <% if(messages.error) { %>
                    <div class="alert alert-danger" role="alert" id='msg'>
                        <%=messages.error%>
                    </div>

                    <% } %>

                        <script>


                            setTimeout(() => {
                                document.getElementById('msg').style.display = 'none'

                            }, 3000)
                        </script>



                        <form method="post">
                            <input class="input" type="text" placeholder="Full Name" required name="Name" />
                            <input class="input" type="text" placeholder="Mobile" required name="Mobile" />
                            <input class="input" type="email" placeholder="Email Address" required name="Email" />
                            <input class="input" type="password" placeholder="Password" required name="Password" />
                            <button type="submit" class="btn">Submit</button>
                        </form>

                        <a href="/Admin_Login" class="back-link">← Back to Login</a>
    </div>
</body>

</html>