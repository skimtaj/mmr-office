<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Mintaj - Login</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
  <style>
    body {
      background-color: #000634;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
      font-family: 'Montserrat', sans-serif;
    }
    .login-container {
      background-color: #0b1047;
      border-radius: 10px;
      padding: 40px;
      box-shadow: 0 8px 16px rgba(0,0,0,0.2);
      max-width: 400px;
      width: 100%;
      margin: 0 auto;
      box-sizing: border-box;
    }
    .login-container h1 {
      font-size: 36px;
      color: #ffffff;
      margin-bottom: 30px;
      text-align: center;
      font-weight: 500;
    }
    .login-container .form-group {
      position: relative;
      margin-bottom: 30px;
    }
    .login-container .form-group label {
      color: #ffffff;
      font-weight: 500;
      position: absolute;
      top: 50%;
      left: 15px;
      transform: translateY(-50%);
      transition: 0.3s ease all;
      pointer-events: none;
      font-size: 16px;
    }
    .login-container .form-control {
      border: none;
      border-radius: 5px;
      background-color: #161E5B;
      color: #ffffff;
      padding: 15px;
      padding-top: 20px;
      padding-left: 15px;
      text-align: left;
    }
    .login-container .form-control::placeholder {
      color: transparent;
    }
    .login-container .form-control:focus {
      box-shadow: none;
      background-color: #161E5B;
    }
    .login-container .form-control:focus ~ label,
    .login-container .form-control:not(:placeholder-shown) ~ label {
      top: -10px;
      left: 15px;
      font-size: 12px;
      color: #0095f6;
    }
    .login-container .input-group {
      position: relative;
    }
    .login-container .input-group .input-group-append {
      position: absolute;
      right: 0;
      top: 0;
      height: 100%;
      z-index: 2;
    }
    .login-container .input-group .input-group-append .btn {
      background-color: #161E5B;
      border: none;
      border-radius: 0 5px 5px 0;
      color: #ffffff;
      cursor: pointer;
      height: 100%;
    }
    .login-container .input-group .input-group-append .btn:hover {
      background-color: #ffffff;
      color: #161E5B;
    }
    .login-container .btn-primary {
      background-color: #0095f6;
      border: none;
      border-radius: 5px;
      padding: 10px;
      color: #ffffff;
      font-weight: 700;
      margin-bottom: 10px; /* Add space below the button */
    }
    .login-container .btn-primary:hover {
      background-color: #007bb5;
    }
    .login-container .text-center {
      margin-top: 20px;
      color: #ffffff;
    }
    .login-container .text-center a {
      color: #0095f6;
      text-decoration: none;
      font-weight: 500;
    }
    .login-container .text-center a:hover {
      text-decoration: underline;
    }
    .login-container .divider {
      margin: 30px 0;
      text-align: center;
      position: relative;
      border-bottom: 1px solid #ddd;
    }
    .login-container .divider::before {
      content: 'Or';
      position: absolute;
      top: -10px;
      left: 50%;
      transform: translateX(-50%);
      background: #0b1047;
      padding: 0 10px;
      color: #ffffff;
    }

    /* Responsive Design */
    @media (max-width: 576px) {
      .login-container {
        padding: 20px;
        margin: 0 15px;
        box-shadow: 0 4px 8px rgba(0,0,0,0.2);
      }
      .login-container h1 {
        font-size: 28px;
      }
      .login-container .btn-primary {
        padding: 10px;
      }
      .login-container .form-group {
        margin-bottom: 15px;
      }
      .login-container .input-group .form-control {
        padding: 12px;
      }
      .login-container .input-group .btn {
        padding: 0 10px;
        height: auto;
      }
    }
  </style>
</head>
<body>
  <div class="login-container">
    <h1>Admin Login</h1>
    <form method="post">
      <div class="form-group">
        <input type="text" class="form-control" id="username" placeholder=" " required name="Email">
        <label for="username">Username or Email</label>
      </div>
      <div class="form-group">
        <div class="input-group">
          <input type="password" class="form-control" id="password" placeholder=" " required name="Password">
          <label for="password">Password</label>
          <div class="input-group-append">
            <button class="btn" type="button" id="togglePassword">
              <i class="fas fa-eye"></i>
            </button>
          </div>
        </div> 
      </div>
      <button type="submit" class="btn btn-primary btn-block">Log In</button>
      <div class="text-center">
        <a href="#">Forgot password?</a>
      </div>
    </form>
    <div class="divider"></div>
    <div class="text-center">
      <p>Don't have an account? <a href="/Admin_Registration">Sign up</a></p>
    </div>
  </div>

  <!-- Scripts -->
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.1/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/js/all.min.js"></script>
  <script>
    document.getElementById('togglePassword').addEventListener('click', function () {
      const passwordField = document.getElementById('password');
      const icon = this.querySelector('i');
      if (passwordField.type === 'password') {
        passwordField.type = 'text';
        icon.classList.remove('fa-eye');
        icon.classList.add('fa-eye-slash');
      } else {
        passwordField.type = 'password';
        icon.classList.remove('fa-eye-slash');
        icon.classList.add('fa-eye');
      }
    });
  </script>
</body>
</html>
