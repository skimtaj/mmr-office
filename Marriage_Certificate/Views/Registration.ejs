<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Admin Register</title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Montserrat:wght@400;700&display=swap">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">
  <style>
    body {
      background-color: #000634;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
      font-family: 'Montserrat', sans-serif;
    }
    .register-container {
      background-color: #0b1047;
      border-radius: 10px;
      padding: 40px;
      box-shadow: 0 8px 16px rgba(0,0,0,0.2);
      max-width: 400px;
      width: 90%;
      margin: 20px;
    }
    .register-container h1 {
      font-size: 36px;
      color: #ffffff;
      margin-bottom: 30px;
      text-align: center;
      font-weight: 500;
    }
    .register-container .form-group {
      position: relative;
      margin-bottom: 20px;
    }
    .register-container .form-group label {
      position: absolute;
      top: 50%;
      left: 15px;
      color: #ffffff;
      font-weight: 500;
      pointer-events: none;
      transition: all 0.3s ease;
      transform: translateY(-50%);
    }
    .register-container .form-control {
      border: none;
      border-radius: 5px;
      background-color: #161E5B;
      color: #ffffff;
      padding: 15px;
      padding-top: 20px; /* Adjusted padding to fit label */
      font-size: 16px;
    }
    .register-container .form-control::placeholder {
      color: transparent;
    }
    .register-container .form-control:focus::placeholder {
      color: transparent;
    }
    .register-container .form-control:focus + label,
    .register-container .form-control:not(:placeholder-shown) + label {
      top: -20px;
      left: 15px;
      font-size: 12px;
      color: #0095f6;
      transform: translateY(0); /* Reset transform */
    }
    .register-container .input-group {
      position: relative;
    }
    .register-container .input-group .input-group-append {
      position: absolute;
      right: 0;
      top: 0;
      height: 100%;
      z-index: 2;
      display: flex;
      align-items: center;
    }
    .register-container .input-group .input-group-append .btn {
      background-color: #161E5B;
      border: none;
      border-radius: 0 5px 5px 0;
      color: #ffffff;
      cursor: pointer;
      height: 100%;
    }
    .register-container .input-group .input-group-append .btn:hover {
      background-color: #ffffff;
      color: #161E5B;
    }
    .register-container .btn-primary {
      background-color: #0095f6;
      border: none;
      border-radius: 5px;
      padding: 10px;
      color: #ffffff;
      font-weight: 700;
    }
    .register-container .btn-primary:hover {
      background-color: #007bb5;
    }
    .register-container .text-center {
      margin-top: 20px;
    }
    .register-container .text-center p {
      color: #ffffff;
      font-weight: 300;
    }
    .register-container .text-center a {
      color: #0095f6;
      text-decoration: none;
      font-weight: 500;
    }
    .register-container .text-center a:hover {
      text-decoration: underline;
    }
    .register-container .divider {
      margin: 30px 0;
      text-align: center;
      position: relative;
      border-bottom: 1px solid #ddd;
    }
    .register-container .divider::before {
      content: 'Or';
      position: absolute;
      top: -10px;
      left: 50%;
      transform: translateX(-50%);
      background: #0b1047;
      padding: 0 10px;
      color: #ffffff;
    }

    /* Responsive Design */
    @media (max-width: 576px) {
      .register-container {
        padding: 20px;
        margin: 10px;
        box-shadow: 0 4px 8px rgba(0,0,0,0.2);
      }
      .register-container h1 {
        font-size: 28px;
      }
      .register-container .form-group label {
        font-size: 14px;
      }
      .register-container .form-control {
        padding: 12px;
      }
      .register-container .btn-primary {
        padding: 10px;
        font-size: 16px;
      }
      .register-container .text-center p {
        font-size: 14px;
      }
      .register-container .text-center a {
        font-size: 14px;
      }
    }
  </style>
</head>
<body>
  <div class="register-container">
    <h1>Register</h1>


    <% if (messages.success) { %>
        <div class="alert alert-success" id="sms"> 
          <%= messages.success %>
        </div>
      <% } %>
    
      <% if (messages.error) { %>
        <div class="alert alert-danger" id="sms">
          <%= messages.error %>
        </div>
      <% } %> 

    <script> 

setTimeout(function(){

document.getElementById('sms').style.display = 'none'

}, 3000 ) ; 

    </script>



    <form method="post">
      <div class="form-group">
        <input type="text" class="form-control" id="name" placeholder=" " required name="Name">
        <label for="name">Full Name</label>
      </div>
      <div class="form-group">
        <input type="text" class="form-control" id="mobile" placeholder=" " required name="Mobile">
        <label for="mobile">Mobile Number</label>
      </div>
      <div class="form-group">
        <input type="email" class="form-control" id="email" placeholder=" " required name="Email">
        <label for="email">Email Address</label>
      </div>
      <div class="form-group">
        <div class="input-group">
          <input type="password" class="form-control" id="password" placeholder=" " required name="Password">
          <label for="password">Password</label>
          <div class="input-group-append">
            <button class="btn" type="button" id="togglePassword">
              <i class="fas fa-eye"></i>
            </button>
          </div>
        </div>
      </div>
      <button type="submit" class="btn btn-primary btn-block">Register</button>
    </form>
    <div class="divider"></div>
    <div class="text-center">
      <p>Already have an account? <a href='/Admin_Login'>Log in</a></p>
    </div>
  </div>

  <!-- Scripts -->
  <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
  <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.1/dist/umd/popper.min.js"></script>
  <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/js/all.min.js"></script>
  <script>
    document.getElementById('togglePassword').addEventListener('click', function () {
      const passwordField = document.getElementById('password');
      const icon = this.querySelector('i');
      if (passwordField.type === 'password') {
        passwordField.type = 'text';
        icon.classList.remove('fa-eye');
        icon.classList.add('fa-eye-slash');
      } else {
        passwordField.type = 'password';
        icon.classList.remove('fa-eye-slash');
        icon.classList.add('fa-eye');
      }
    });
  </script>
</body>
</html>
