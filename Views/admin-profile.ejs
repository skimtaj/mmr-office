<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">

    <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@3.3.7/dist/css/bootstrap.min.css"
        integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <style>
        body {
            margin: 0;
            font-family: 'Roboto', sans-serif;
            background: #495057;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            flex-direction: column;
        }

        .profile-card {
            background-color: #fff;
            border-radius: 15px;
            padding: 20px;
            width: 100%;
            max-width: 360px;
            text-align: center;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            position: relative;
            overflow: hidden;
            margin: 20px;
        }

        .profile-card::before {
            content: '';
            width: 100%;
            height: 120px;
            background: linear-gradient(135deg, #4A90E2, #007AFF);
            position: absolute;
            top: 0;
            left: 0;
        }

        .profile-card img {
            width: 100px;
            height: 100px;
            border-radius: 50%;
            border: 5px solid #fff;
            margin-top: 60px;
            z-index: 1;
            position: relative;
        }

        .profile-card h1 {
            margin: 10px 0 5px;
            font-size: 24px;
            color: #333;
        }

        .profile-card p {
            font-size: 14px;
            color: #777;
            margin: 5px 0;
        }

        .stats {
            display: flex;
            justify-content: space-between;
            margin: 20px 0;
        }

        .stats div {
            text-align: center;
        }

        .stats div span {
            display: block;
            font-size: 18px;
            font-weight: bold;
            color: #333;
        }

        button {
            background-color: #007AFF;
            border: none;
            padding: 10px 20px;
            color: #fff;
            border-radius: 25px;
            cursor: pointer;
            font-size: 14px;
            margin-top: 10px;
            transition: background-color 0.3s;
        }

        button:hover {
            background-color: #005BB5;
        }

        .action-icons {
            display: flex;
            justify-content: space-between;
            margin-top: 15px;
            padding: 0 20px;
        }

        .action-icons a {
            color: #007AFF;
            text-decoration: none;
            font-size: 16px;
            display: flex;
            align-items: center;
        }

        .action-icons a i {
            margin-right: 8px;
        }

        .action-icons a:hover {
            color: #005BB5;
        }

        .back-button {
            background-color: #4A90E2;
            border: none;
            padding: 10px 20px;
            color: #fff;
            border-radius: 25px;
            cursor: pointer;
            font-size: 14px;
            margin-top: 20px;
            transition: background-color 0.3s;
            display: inline-block;
            text-decoration: none;
        }

        .back-button:hover {
            background-color: #005BB5;
        }

        /* Responsive Styles */
        @media (max-width: 576px) {
            .profile-card {
                width: 90%;
                padding: 15px;
            }

            .profile-card img {
                width: 80px;
                height: 80px;
            }

            .profile-card h1 {
                font-size: 20px;
            }

            .profile-card p {
                font-size: 12px;
            }

            .stats {
                display: block;
            }

            .stats div {
                margin-bottom: 15px;
            }

            .action-icons {
                flex-direction: column;
                align-items: center;
            }

            .action-icons a {
                margin-bottom: 10px;
            }
        }
    </style>
</head>

<body>

    <div class="profile-card">

        <img src="/upload/<%=adminSourse.Profile_Image%>" alt="User Profile Picture">

        <% if (messages.success) { %>
            <div class="alert alert-success" id="msg">
                <%= messages.success %>
            </div>
            <% } %>

                <% if (messages.error) { %>
                    <div class="alert alert-danger" id="msg">
                        <%= messages.error %>
                    </div>
                    <% } %>

                        <h1>
                            <%= adminSourse.Name %>
                        </h1>
                        <p><b>Mobile :</b>
                            <%= adminSourse.Mobile %>
                        </p>
                        <p><b>Email :</b>
                            <%= adminSourse.Email %>
                        </p>

                        <a href="/mmr-office/edit-profile" class="back-button">Edit User</a>
                        <a href='/mmr-office/admin-dashboard' class="back-button">Back</a>

    </div>

    <script>
        setTimeout(() => {
            document.getElementById('msg').style.display = 'none';
        }, 3000);
    </script>

</body>

</html>