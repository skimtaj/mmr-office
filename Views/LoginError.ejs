<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Error</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }
        .modal-dialog {
            max-width: 400px;
            transition: transform 0.3s ease-in-out;
        }
        .modal-dialog.show {
            transform: scale(1);
        }
        .modal-content {
            border-radius: 10px;
            animation: fadeIn 0.3s ease-in-out;
        }
        .modal-header {
            background-color: #dc3545;
            color: white;
            border-bottom: none;
        }
        .modal-body {
            text-align: center;
            font-size: 1.1rem;
        }
        .modal-footer {
            border-top: none;
            justify-content: center;
        }
        .modal-body i {
            font-size: 3rem;
        }
        .btn-ok {
            background-color: #dc3545;
            color: white;
            border: none;
            border-radius: 5px;
            padding: 0.5rem 1rem;
        }
        .btn-ok:hover {
            background-color: #c82333;
        }
        @keyframes fadeIn {
            from {
                opacity: 0;
            }
            to {
                opacity: 1;
            }
        }
    </style>
</head>
<body>

<!-- Modal -->
<div class="modal show d-block" tabindex="-1" aria-labelledby="errorModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="errorModalLabel">Login Error</h5>
            </div>
            <div class="modal-body">
                <i class="bi bi-exclamation-circle-fill text-danger"></i>
                <p class="mt-3">Invalid Username or Password</p>
            </div>
            <div class="modal-footer">
                <a class="btn btn-danger"  href="/Student_Login" role="button">Back To Login </a>
            </div>
        </div>
    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.min.js"></script>
</body>
</html>
